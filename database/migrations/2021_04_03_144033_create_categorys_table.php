<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;
use App\Models\Category;

class CreateCategorysTable extends Migration
{
    /**
     * Run the migrations.
     *
     * @return void
     */
    public function up()
    {
        Schema::create('categorys', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('user_id');
            $table->foreign('user_id')->references('id')->on('users')->comment('User ID người tạo');;
            $table->string('name', 255);
            $table->string('slug')->unique()->index();
            $table->enum('status', [Category::STATUS_WAIT_ACTIVATION, Category::STATUS_ACTIVATED, Category::STATUS_DEACTIVATED])->default(Category::STATUS_WAIT_ACTIVATION);
            $table->softDeletes();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     *
     * @return void
     */
    public function down()
    {
        Schema::dropIfExists('categorys');
    }
}
